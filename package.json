{
  "name": "moanete-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser-based meeting assistant — Chrome extension with PiP overlay",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check": "biome check .",
    "format": "biome format --write .",
    "lint": "biome lint .",
    "fix": "biome check --fix ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.0.0"
  }
}
